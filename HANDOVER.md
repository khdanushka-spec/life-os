# Handover — 2026-07-27 (Premium design system overhaul — Phase 1, deployed; live feedback in progress)

## Goal

Phase 4 (all 7 life-area modules) shipped in the prior session. This session is a **separate, explicitly-scoped effort**: Dhanu asked for a full premium UI/UX redesign (Apple/Linear/Arc/Raycast/Notion/Vercel-tier) across the entire app, with a detailed spec (dark near-black theme, real accent color, typography scale, motion everywhere, command palette, etc.) and an explicit instruction to work module-by-module rather than a shallow all-at-once pass.

A plan was written and approved (`C:\Users\dnand\.claude\plans\fluttering-exploring-grove.md` — read it for full reasoning) scoping this session to three things that cascade to 100% of the app automatically: **1)** design system foundation, **2)** App Shell (Sidebar+Topbar), **3)** Home dashboard. All 9 other module pages (Tasks first, then Habits/Journal/Finance/Work/Health/Learning/Family/Travel/Vault) are explicitly deferred to follow-up sessions, one at a time.

## State

**Phase 1 is built, committed, pushed, and deployed to production** (`aura.dkns.ai`). Dhanu has now seen it live and given two rounds of real feedback:

1. She hit a **crash on Home** (screenshot: Next.js error boundary, "Something went wrong"). Root-caused via `vercel logs` (Vercel MCP `get_runtime_errors`/`get_runtime_logs` are 403-forbidden in this environment's token scope — use the CLI: `npx vercel logs <deployment-url>` instead, it works) and fixed. **This is resolved and deployed** — see commit `510ec97` below.
2. She then sent a screenshot of the **Tasks page's hero stat row** (`Focus Score 100%`, `Today's Tasks 0`, `Completed 0`, `Overdue 0`, `Weekly Progress 0%` — all flat white-on-black, no color differentiation) with the feedback: *"all tabs i need more highlight usign diffrent colours"* (sic). Fixed and deployed (`57571d5`) with a first-pass icon-chip treatment. She then immediately sent **3 more unprompted screenshots** (no caption): Journal's hero (still flat, unfixed), Travel's stats row (already colored), Vault's stats row (already colored) — this revealed that **Habits/Family/Learning/Health/Work/Travel/Vault already had a colored stat-card pattern from Phase 4** (icon top-right, tinted background wash, animated count-up number, duplicated near-identically across all 7 files) that the Tasks/Journal heroes just never got. **Fully resolved this session**: extracted that duplicated pattern into `src/components/stat-card.tsx`, refactored all 7 existing modules onto it, fixed Journal's `today-card.tsx` (was completely uncolored) to use it, and redid Tasks's hero to use the same shared component instead of the one-off icon-chip style from the first fix — so all 9 module hero/stats rows are now visually consistent. Committed (`1157936`), pushed, deployed to production.
3. Then a crash on `/tasks` and `/journal` (see `d4f56da` in Commits), then a tagline request (`2d8ebf3`), then: **"full side and all modules very slow. I need to site speed."** — a genuine, separate performance complaint, not tied to a specific screenshot. Root-caused three compounding, site-wide causes (not a per-module issue) — see `6b253e4` in Commits for the full breakdown: (1) every page independently re-ran the full auth check the `(app)` layout had already done, undeduped; (2) Vercel functions ran in `iad1` (US East) while the Supabase DB is in `ap-northeast-2` (Seoul) — full cross-Pacific round-trip on every query; (3) Home and Journal ran their ~15-20 DB queries mostly sequentially instead of fanned out in parallel. All three fixed and deployed. Not yet confirmed by Dhanu whether it actually feels faster.
4. Then a real feature request from the Aura Brain chat screenshot: she wants natural-language commands like "add tomorrow I have task..." to actually create the task (filed under the right project/tag), and "delete X" to actually delete it — not just have the AI describe what it would do. Built and shipped as `addTask`/`deleteTask` AI-SDK tools (`0c3d487`, see Commits) wired into the existing `/api/chat` route. **First Aura Brain capability beyond read-only Q&A** — deployed. A genuine git-push credential problem (see Gotchas — this machine's two GitHub accounts) meant Dhanu had to run `gh auth login` and `git push` herself from her own terminal before this could ship; **this became the standing workflow for the rest of the session** — this session's tools can commit locally but never push (see Gotchas), so every commit below followed the same pattern: commit here, ask Dhanu to `git push origin main` in her own terminal, then deploy once she confirms.
5. **A long, connected Finance feature arc followed** (Dhanu working through the Finance module screenshot by screenshot), all shipped and deployed:
   - **Sri Lankan/Foreign Accounts** (`1243875`→`540a02e`): a new Finance tab for accounts/investments held in a foreign currency, auto-converted to AUD via a daily-refreshed free FX API (`src/lib/fx.ts`) and rolled into net worth everywhere it's computed. First built LKR-only per her literal words, then generalized to any currency once she asked to rename it "Foreign Accounts" (a rename-only change would have been misleading since the feature was hardcoded to one currency).
   - **Currency-blind balance bug** (`a805889`): adding a real LKR account exposed that two *other* finance calculations (the 30-day cash-flow projection, the daily net-worth snapshot feeding the health score) summed raw balances with zero currency conversion — a raw LKR figure got added to an AUD total as if it were AUD, producing a **~$11.4M** cash-flow projection Dhanu spotted immediately. Fixed both, and cleared the already-cached-wrong values for that day directly in the DB so the fix took effect immediately rather than tomorrow.
   - **Real auto-pay** (`dd1291e`): `RecurringPayment.autoPay` existed in the schema but did nothing - added a nominated `accountId`, and `processDueRecurringPayments()` (runs lazily on Finance page loads, no cron in this app) now actually creates the Transaction and adjusts the account balance when a bill comes due.
   - **Account name/balance edit** (`e39677b`): accounts could previously only be created or archived.
   - **Urgent low-balance alerts** (`dcbd3ef`): projects each liquid account 3 days forward using its nominated recurring payments and shows a destructive `Alert` on the Finance dashboard if it would cross below $200.
   - **Real account-to-account transfers** (`17ea871`): `TransactionType.TRANSFER` existed but was a documented no-op ("doesn't move balance between two accounts"). Now creates a signed paired-row transaction (source negative, destination positive, linked via a new `transferAccountId` field) with correct paired deletion. **Also found and fixed the actual root cause of a raw-UUID-in-dropdown bug** Dhanu screenshotted: Base UI's `<Select.Value>` (unlike Radix) doesn't auto-resolve a label from `<Select.Item>` children — it needs an explicit children render-function or `items` prop, confirmed by reading Base UI's own source. Fixed in the 3 Select instances touched this arc; **~12 more instances elsewhere in the app have the same latent bug, not yet fixed** (see Next Steps).
   - Every one of these mutation-touching changes was verified against the **real production database** with a throwaway-create-verify-delete script (not just `tsc`/`eslint`/`build`), since this is real money math with no way to click through the UI locally.

## Key decisions

- **The single biggest design finding**: every color in this app was pure grayscale (zero OKLCH chroma) before this session — no accent color anywhere, on any module. That's the real reason it read as generic, and it's why a token-level fix in `globals.css` cascades to all ~150 components without touching per-module code.
- Design-token pass + App Shell + one flagship page (Home) = 100% of the app gets a visual lift while producing a small number of fully-realized screens, rather than a shallow pass across all eleven modules shipping half-finished everywhere. This is the core strategic bet of the whole redesign — see the plan file.
- No new npm dependencies (command palette hand-rolled on the existing `Dialog`, not `cmdk`) and no new AI calls/database tables — explicit boundaries in the approved plan, still holding.
- Dhanu's "more highlight using different colours" feedback, read literally against the Tasks screenshot, is specifically about **per-stat-card color coding** (e.g. Overdue in red/warning tones, Completed in success green, Focus Score in the primary blue, etc.) rather than the overall theme — the dark/blue-accent theme itself hasn't been criticized. Her "all tabs" phrasing means this pattern (flat, undifferentiated stat cards) likely repeats in other modules' hero/stats-row components too (Work's `work-stats-row.tsx`, Health's `health-stats-row.tsx`, Learning's `learning-stats-row.tsx`, Family's `family-stats-row.tsx`, Travel's `travel-stats-row.tsx`, Vault's `vault-stats-row.tsx` all follow a similar pattern from Phase 4) — worth a broader color-coding pass, not just Tasks, once Tasks establishes the right visual language to reuse.

## Files touched this session

- `src/app/globals.css` — new dark-first OKLCH palette (near-black bg, layered card/popover, soft blue `--primary`/`--ring`/`--sidebar-primary`, new `--success`/`--warning` tokens, real `--chart-1..5`), `--radius` 0.625rem→0.75rem, new `--shadow-card`/`--shadow-card-hover`/`--shadow-glow-primary` vars, new `.text-display/.text-heading/.text-subheading/.text-caption/.text-metric` utility classes, new `--animate-shimmer` keyframe. **Finished.**
- `src/lib/motion.ts` (new) — shared Framer Motion variants (`fadeInUp`, `fadeIn`, `staggerContainer`, `hoverLift`, `EASE_OUT`, duration constants). **Finished.**
- `src/components/empty-state.tsx` (new) — shared empty-state component. **Finished, and already had one real bug fixed post-deploy** (see Gotchas).
- `src/components/ui/card.tsx`, `button.tsx`, `skeleton.tsx` — hover lift/shadow, `loading` prop, shimmer sweep. Same APIs, additive only. **Finished.**
- `src/components/circular-progress.tsx` — gradient stroke (`useId()`-scoped), entrance animation, `useReducedMotion()` respected, 64→72px. **Finished.**
- `src/app/layout.tsx` — `next-themes` `defaultTheme` → `"dark"`. **Finished.**
- `src/components/app-shell/desktop-sidebar.tsx`, `sidebar-nav.tsx` — animated active indicator, hover glow, section grouping, collapsible rail (`useSyncExternalStore` + `localStorage`). **Finished.**
- `src/components/app-shell/topbar.tsx`, `command-palette.tsx` (new) — restyled topbar, `⌘K` command palette hand-rolled on `Dialog`+`Input`. **Finished.**
- `src/app/(app)/home/page.tsx`, `src/components/home/home-hero.tsx`, `momentum-rings.tsx`, `ai-suggestions-card.tsx` (new) — real weather via existing `getBrisbaneWeather()`, real (not fabricated) daily-summary sentence via new `buildDailySummary()`, habits trend badge, `EmptyState` for Today's Focus/Smart Timeline. Same underlying Prisma queries. **Finished.**
- `src/components/stat-card.tsx` (new) — shared `StatCard` + `AnimatedNumber`, extracted from 7 near-identical copies. `StatCard`'s `value` prop accepts `number | string` (numbers get the Framer Motion count-up + `text-3xl`; strings render as plain `text-lg`, for cases like Journal's Mood/Energy). Takes an `accent` prop (any Tailwind `bg-*` class, semantic like `bg-success`/`bg-destructive` or decorative like `bg-sky-500`) rendered as a 10%-opacity full-card background wash. **Finished.**
- `src/components/tasks/tasks-hero.tsx`, `src/components/journal/today-card.tsx`, `src/components/family/family-stats-row.tsx`, `src/components/habits/habit-stats-row.tsx`, `src/components/health/health-stats-row.tsx`, `src/components/learning/learning-stats-row.tsx`, `src/components/travel/travel-stats-row.tsx`, `src/components/vault/vault-stats-row.tsx`, `src/components/work/work-stats-row.tsx` — all now import `StatCard` from `stat-card.tsx` instead of each defining their own copy. Journal's `TodayCard` (previously the one hero with zero color logic) and Tasks's hero (previously a different, one-off icon-chip pattern) both now use the same component as everyone else. Net **-329 lines** removing duplication. **Finished, deployed.**
- `src/server/db-user.ts` — wrapped `resolveDbUserFromSession` in React's `cache()`. **Finished, deployed.**
- `vercel.json` — added `"regions": ["icn1"]`. **Finished, deployed, confirmed via `vercel inspect aura.dkns.ai` that functions now build as `[icn1]` instead of `[iad1]`.**
- `src/app/(app)/home/page.tsx` — `getDashboardData()`'s ~15 sequential/semi-parallel queries collapsed into one `Promise.all` (weather + `requireDbUser()` first since the rest need `dbUser.id`, then everything else at once). Same query shapes and logic, just fired concurrently instead of section-by-section. **Finished, deployed.**
- `src/app/(app)/journal/page.tsx` — merged 3 sequential `Promise.all` groups (entries/dates/today/weather, then reflection/insights, then weekly/monthly reports) into 1, since none of them actually depended on each other's results. **Finished, deployed.**
- `src/lib/ai/chat-tools.ts` (new) — `buildChatTools(userId)` returns an AI-SDK `ToolSet` with `addTask` (reuses the existing `createTaskAction`, resolves an optional project name to an existing project by case-insensitive match) and `deleteTask` (matches by title substring among the user's active tasks; deletes immediately only on an unambiguous single match via the existing `deleteTaskAction`, otherwise returns candidates instead of guessing). **Finished, deployed.**
- `src/app/api/chat/route.ts` — wired `tools: buildChatTools(dbUser.id)` and `stopWhen: stepCountIs(5)` into the `streamText` call so the model can call a tool then keep generating a confirmation message in the same turn. Also added today's Brisbane date to the system prompt (it previously had no way to resolve "tomorrow"/relative dates) and a paragraph telling the model to actually call the tools rather than just describe an intention, and to ask for clarification rather than guess when `deleteTask` returns multiple candidates. **Finished, deployed.**

## Commits this session

- `c8285e9` — "Premium design system overhaul: foundation, shell, Home (Phase 1)" — pushed and deployed.
- `510ec97` — "Fix Home page crash: EmptyState can't take a component reference as a prop" — pushed and deployed, confirmed via `vercel logs` that the specific error class is gone (see Gotchas for the exact bug).
- `57571d5` — "Color-code Tasks hero stat cards" — first-pass fix, superseded in visual style (not reverted) by `1157936` below.
- `918bfe5` — handover doc update after the first Tasks fix.
- `1157936` — "Fix Journal's flat hero stats, align Tasks with the shared stat-card pattern" — pushed and deployed. **This commit accidentally reintroduced the exact same RSC crash class as `510ec97`** — see next commit and Gotchas.
- `ed1a2e0` — handover doc update after the stat-card sweep (written before the crash below was discovered).
- `d4f56da` — "Fix production crash on /tasks and /journal: missing 'use client'" — Dhanu hit "Something went wrong" on `/tasks` immediately after `1157936` deployed. Root cause via `vercel logs --level error --since 30m --json`: `today-card.tsx` and `tasks-hero.tsx` are Server Components (no `"use client"`) passing `lucide-react` icon component references into `StatCard` (a `"use client"` component) — functions aren't RSC-serializable, identical bug class to the `EmptyState` crash from `510ec97`. Every other stats-row file already had `"use client"` at the top for this exact reason (they used to define their own local copy of `StatCard` inside the same client file); these two were new files that didn't inherit it. Fixed by adding `"use client"` to both. Pushed and deployed; confirmed no new error-level logs in the 5 minutes after deploy, but could not click through to `/tasks` or `/journal` myself to fully confirm (no login credentials — see Open Questions).
- `2d8ebf3` — "Add 'Your Life, Organized.' tagline under the AURA OS brand mark" — Dhanu asked for this directly. Sidebar header + topbar both grew `h-14`→`h-16` to fit a second line without clipping (kept their border lines aligned across the shell), and the one place that hardcoded the old topbar height (`ai/page.tsx`'s `h-[calc(100svh-3.5rem)]` chat viewport) was updated to `4rem`. Also added to the mobile nav sheet via `SheetDescription` for consistency. Pushed and deployed; not visually verified (same login-credential gap).
- `6b253e4` — "Fix site-wide slowness: dedupe auth check, co-locate DB region, parallelize queries" — Dhanu: *"full side and all modules very slow. I need to site speed. please do it."* Investigated from first principles rather than guessing: checked `vercel inspect` (functions in `iad1`), the Supabase `DATABASE_URL` (`ap-northeast-2.pooler.supabase.com` — Seoul), grepped every module page for `requireDbUser`/`getDbUser` calls (59 files — every single page calls it independently, on top of the `(app)/layout.tsx` call, with no memoization), and read through Home's and Journal's data-fetching for sequential-query chains. Three fixes, all pushed and deployed: `cache()`-wrapped the auth resolver (db-user.ts), pinned Vercel functions to `icn1` in `vercel.json` (verified via `vercel inspect` post-deploy — functions now show `[icn1]`), and collapsed Home's ~15-query chain and Journal's 3 sequential `Promise.all` groups into single fan-outs. **First draft of this same commit accidentally introduced a real bug while trying to preserve query-ordering semantics** (fired every Home query twice — once without a `userId` filter, which would've leaked all users' data, then again correctly filtered) — caught on self-review before committing, not shipped. See Gotchas for what the correct pattern looks like.
- `0c3d487` — "Give Aura Brain the ability to add/delete tasks from chat commands" — new `addTask`/`deleteTask` AI-SDK tools, see Files touched. **This commit's push required a full session detour** — see the Gotchas entry on the two-GitHub-accounts credential problem. Pushed (by Dhanu herself, from her own terminal, not this session's tools) and deployed. Confirmed by Dhanu as working.
- `bb4cd74` — handover doc update.
- `c769e81` — "Perf: lazy-load TipTap editors, enable client Router Cache for dynamic routes" — separate perf-focused ask ("Optimize the entire project for maximum speed"). TipTap/ProseMirror (~880KB combined) was in the initial bundle for Journal/Task-detail/Vault-detail even for read-only visits; moved behind `next/dynamic(..., {ssr:false})` wrapper files (`*-lazy.tsx`) since Server Component pages can't set `ssr:false` directly. Also `experimental.staleTimes.dynamic = 30` in `next.config.ts` (was 0, meaning zero client-side page-revisit caching). Confirmed via build-manifest diff that the TipTap chunks left the four routes' initial bundles.
- `1243875` → `540a02e` — Sri Lankan Accounts, generalized to Foreign Accounts. See State #5.
- `a805889` — currency-blind balance bug fix + live-cache-clear. See State #5.
- `dd1291e` — real auto-pay (nominated account, `processDueRecurringPayments`). See State #5.
- `e39677b` — account name/balance edit dialog.
- `dcbd3ef` — urgent low-balance alerts on the Finance dashboard.
- `17ea871` — real account-to-account transfers + the Base UI `SelectValue` bug fix. See State #5.

## Gotchas / constraints learned

- **Vercel MCP tools (`get_runtime_errors`, `get_runtime_logs`) return 403 Forbidden** in this environment (token scope issue — "Not authorized: Trying to access resource under scope 'dkns'"). Use the CLI instead: `npx vercel logs <deployment-url>` (a specific deployment URL, or the production alias `aura.dkns.ai`) tails real runtime logs including thrown errors with digests. This is how the EmptyState crash was root-caused — the production error page itself omits error details, but the CLI log line had the full stack: `Error: Functions cannot be passed directly to Client Components unless you explicitly expose it by marking it with "use server"`.
- **Never pass a bare component reference as a prop from a Server Component into a Client Component.** `EmptyState` (a `"use client"` component) was receiving `icon={ListChecks}` from `home/page.tsx` (an async Server Component) — a component type is a function, and functions aren't serializable across the RSC server/client boundary. Fixed by changing the prop to take a rendered element (`icon={<ListChecks />}` instead of `icon={ListChecks}`) — a JSX element is a plain serializable object. `EmptyState`'s `icon` prop type is now `ReactNode`, and it wraps whatever's passed in a `<span className="[&>svg]:size-6 [&>svg]:text-primary">` so callers don't need to size/color the icon themselves. **Any future module adopting `EmptyState` from a Server Component page must pass `icon={<SomeIcon />}`, not `icon={SomeIcon}`** — this is an easy mistake to repeat since it type-checks fine either way (TypeScript doesn't catch this; it's a runtime-only RSC serialization failure).
- **This bug bit twice in one session** (`510ec97` then `d4f56da`), same root cause, different component: `StatCard` (`src/components/stat-card.tsx`) takes `icon: React.ComponentType<...>` and expects to receive a component reference like `icon={Flame}`, not a rendered element — that's intentional (it needs the bare component to control the wrapper/sizing itself). That means **every file that renders a `<StatCard icon={SomeIcon} .../>` must itself be a `"use client"` component**, or the `icon` prop crosses the Server→Client boundary unserialized and crashes at runtime with no build-time warning. `tsc`, `eslint`, and `next build` are all silent on this class of bug — it only surfaces via `vercel logs` after a real request hits production (or reproducing it locally with a working login). **Before adding any new `<StatCard>` usage or any other component with a bare `icon: ComponentType` prop, check that the file has `"use client"` at the top first.**
- This project's ESLint config enforces `react-hooks/set-state-in-effect` strictly (error, not warning). Fixes by scenario, all applied this session in the sidebar/command-palette work:
  - External store read on mount (e.g. `localStorage`) → `useSyncExternalStore`, not `useState`+`useEffect` (also avoids a hydration mismatch).
  - State reset when a *prop* changes → the React-documented "adjust state during render" pattern (track previous prop in state, compare during render, conditionally `setState` in the render body).
  - State reset from a *local* event → just do it in the event handler directly, no effect at all.
  - Effects should only remain for genuine external-system sync (event listeners, imperative `.focus()` calls).
- Tailwind v4's `@theme` block can register `@keyframes`-backed animations: `--animate-<name>: <name> <duration> <timing> <iteration>;` plus a matching top-level `@keyframes <name> {}` — then it's a plain utility class (`animate-shimmer`), no arbitrary-value syntax needed.
- `useId()`-scoped SVG `<linearGradient id={...}>` is required for any gradient-stroked SVG component that might render multiple instances on one page (e.g. `CircularProgress` in Home's momentum rings) — a hardcoded gradient `id` collides and every instance renders whichever def painted last.
- Bash/PowerShell tool cwd sometimes resets to `C:\Users\dnand` (the parent) instead of staying in `aura-os` between calls in this environment — if `tsc`/`prisma`/etc. suddenly can't find their config, `cd /c/Users/dnand/aura-os` first before assuming something is actually broken.
- **When collapsing sequential-then-dependent queries into a single `Promise.all`, don't try to fire the dependent queries before their dependency resolves.** Home's dashboard data needs `dbUser.id` to scope literally every query - the only correct shape is `const [weather, dbUser] = await Promise.all([...])` first (since weather doesn't need the user), *then* a second `Promise.all` for everything that needs `dbUser.id`. A same-session first attempt tried to fire all ~17 queries in one single `Promise.all` including `requireDbUser()` itself, which meant the `userId`-scoped queries literally could not have `dbUser.id` yet - the broken draft filtered by nothing instead (a cross-user data leak) and needed a second, correctly-scoped fetch after. Two DB round trips of parallel work beats one round trip that leaks data or one that's forced fully sequential - don't try to cheat past a genuine data dependency.
- **This app has no local dev-login path**, so performance changes like the `icn1` region pin and query-parallelization in this session were verified via `tsc`/`eslint`/`next build` (correctness) plus `vercel inspect` (confirming the region actually changed) and `vercel logs --level error` (confirming no new runtime errors) — but the actual felt speedup has not been measured with real timing data (no APM/timing tool available in this environment). If Dhanu says it's still slow, the next diagnostic step is asking her which specific page/action feels slow, then checking `vercel logs --query "status:5xx"` or wiring up basic `console.time` around a suspect query rather than assuming these three fixes were sufficient.
- **CRITICAL — `git push` cannot work from this session's Bash/PowerShell tools on this machine, only `git fetch`/read operations can.** This machine has two GitHub accounts (`dhanu-af` and `khdanushka-spec` — this has also bitten the separate `dev-launcher`/Brain project on this machine before), `aura-os`/`life-os` belongs to `khdanushka-spec`, and this session's tool environment's cached credential (via `gh` CLI, which Git Credential Manager delegates to — checked `gh auth status`, and the raw `%APPDATA%\GitHub CLI\hosts.yml`) is stuck on `dhanu-af`. Deleting the Windows Credential Manager entry (`cmdkey /delete:LegacyGeneric:target=git:https://github.com`) is blocked by this environment's own permission classifier as a sensitive action, and even when it's deleted, `gh`'s own separately-stored token is what's actually used. **The real finding this session: Dhanu ran `gh auth login` successfully as `khdanushka-spec` in her own real terminal (visible to her, confirmed "✓ Logged in as khdanushka-spec"), but this session's tools still saw only `dhanu-af` in `gh auth status` and `hosts.yml` afterward** — meaning this session's Bash/PowerShell tools run in an environment that does NOT share credential state (and possibly not the whole user profile) with the real interactive terminal Dhanu types into, even though both report the same `C:\Users\dnand` paths and `git fetch` from both sides shows the same repo state. **This became the standing workflow for every commit for the rest of the session and should be assumed permanent**: commit locally here, tell Dhanu "please run `git push origin main`" in her own terminal, wait for her "done", `git fetch origin main` to confirm, then deploy via `npx vercel deploy --prod --yes`. Don't waste a turn re-attempting `git push` from this session's tools first — it has failed 100% of the time this session.
- **This project has no `prisma/migrations` directory** — every schema change this session (Foreign Accounts' pre-existing `currency` field needed no change; auto-pay's `RecurringPayment.accountId`; transfers' `Transaction.transferAccountId`) was applied with `npx prisma format` → `npx prisma validate` → `npx prisma db push` → `npx prisma generate`, directly against the live production database (there's only one DB — no separate dev/staging). This is the established convention for this project, not a shortcut being taken here; keep using it rather than introducing a migrations directory unprompted.
- **Real-money verification pattern established this session, worth repeating for any future finance mutation logic**: `tsc`/`eslint`/`next build` catch type errors, not wrong math or wrong direction of a balance update. For auto-pay, transfers, and the account-edit action, a throwaway `tsx` script (in the repo root, deleted after) created real-but-clearly-named (`TEST-DELETE-ME`) rows via a real Prisma client against the actual production DB, ran the exact logic being verified, asserted the resulting numbers, then deleted every trace. This caught nothing wrong this session, but it's the reason to trust these features are correct rather than "probably correct" — there's no login to click through the UI and check by hand, so this is the only real verification available. Keep doing this for anything that touches account balances.
- **Base UI's `<Select.Value>` (this project's dropdown primitive, `@base-ui/react/select`) does NOT auto-resolve a label from `<Select.Item>` children the way Radix does.** Read the actual source (`node_modules/@base-ui/react/select/value/SelectValue.js` → `resolveSelectedLabel` in `internals/resolveValueLabel.js`) rather than guessing: without an `items` prop on `<Select.Root>` or a children render-function on `<Select.Value>`, it falls back to displaying the raw `value` as text. This is why Dhanu saw a raw account UUID in a dropdown trigger. **Fix pattern**: `<SelectValue>{accounts.find(a => a.id === value)?.name}</SelectValue>` (or a lookup function) instead of bare `<SelectValue />`. Fixed in `transaction-form.tsx`, `recurring-form.tsx`, `recurring-list.tsx` this session. **Still broken in ~12 other files** (grep for `<SelectValue` with no children/`items` — as of this session: `account-form.tsx`, `budget-form.tsx`, `investment-form.tsx` in `finance/`, plus files under `health/`, `learning/`, `travel/`, and `tasks/task-board.tsx`/`task-detail-fields.tsx`/`task-toolbar.tsx`). Most of these have value/label pairs that read *plausibly* (e.g. `"WEEKLY"` instead of `"Weekly"`) rather than obviously-broken like a UUID, so they've likely gone unnoticed rather than being fine — worth a dedicated sweep.

## Next steps

1. **Sweep the remaining ~12 `<SelectValue />` instances with the same raw-value display bug** (see Gotchas for the exact list and fix pattern) — same bug Dhanu already screenshotted once for accounts; likely to surface again for interval/type dropdowns elsewhere if not fixed proactively.
2. **None of the Finance feature work this session has been visually confirmed by Dhanu** — Foreign Accounts, the auto-pay nominated-account picker, account edit, low-balance alerts, and transfers were all verified via `tsc`/`eslint`/`build` plus real-database money-math scripts (see Gotchas), but never clicked through in the actual UI (no login). Watch for her next round of screenshots for anything that looks or behaves wrong.
3. **Finance's hero/stats row has still never been explicitly checked** — carried over from earlier in the session, still not done. Quick look next time since it's likely already fine (same Phase 4 batch as the other modules that already had it).
4. The **Tasks page full redesign** (smart filters sidebar, priority colors/status chips, right-rail AI assistant/focus timer/analytics) is still not started — was next in line before the Finance arc took over the rest of the session. Read `src/app/(app)/tasks/page.tsx` and `src/components/tasks/*` first; `task-sidebar.tsx`, `right-sidebar.tsx`, `focus-timer.tsx` already exist and likely need restyling more than rebuilding.
5. Then Habits, Journal, Finance, Work, Health, Learning, Family, Travel, Vault module pages one at a time for the Phase 1 typography/motion treatment (hero/stats rows are done; the rest of each page — lists, forms, detail views — isn't).
6. **Whenever adding a new component that takes a bare `icon: ComponentType` prop (like `StatCard`), double-check `"use client"` is present on every file that renders it** — caused two production crashes earlier this session (`510ec97`, `d4f56da`).
7. **Cross-currency transfers aren't supported** (transfer form requires both accounts to share a currency, rejects silently otherwise) — fine for now since Dhanu's example was AUD-to-AUD, but worth building properly (reusing `lib/fx.ts`) if she asks to transfer between an AUD and a Foreign account.
8. **Auto-pay has no true cron** — it catches up lazily on Finance page loads, same-day-accurate for a daily-use app but not midnight-precise. A real Vercel Cron would need a `CRON_SECRET`-protected route handler; not built since it's unverifiable without waiting for a scheduled fire, but worth doing if bills ever get missed because she didn't open the app that day.

## Open questions

- Do the Finance features from this session actually work as expected once Dhanu tries them for real? Genuinely unknown — see Next Steps #2.
- Does the site still feel slow anywhere specific? Never got explicit confirmation either way before she moved on to the Finance requests — assume resolved unless she says otherwise.
- No known local dev-login credentials exist for this app (real email/password auth, not just the `SUPER_ADMIN_USERNAME`/`SUPER_ADMIN_PASSWORD` seeded via `prisma/seed.ts`) — visual browser verification has to happen either against production after deploy, or by asking Dhanu for a way to check locally. This has now blocked visual confirmation on essentially every UI change across the whole session — worth asking Dhanu directly whether a local seed user/test account can be set up, so this stops being a recurring gap every single session.
